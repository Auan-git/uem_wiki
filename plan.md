# MD转HTML模板系统设计方案

## 目标
建立一个模板系统，使得修改MD文件后，只需运行一个脚本即可自动更新对应的HTML文件。

## 现有HTML文件结构分析

每个HTML文件包含以下共同部分：

1. **头部（head）** - 包含CSS样式
2. **导航栏（nav）** - 顶部导航
3. **侧边栏（sidebar）** - 左侧导航菜单
4. **主内容区（markdown-section）** - 文章内容
5. **评论区（twikoo）** - 评论系统
6. **脚本（scripts）** - JavaScript功能

## 设计方案

### 1. 创建模板文件

创建 `templates/` 目录，包含：
- `base.html` - 基础模板，包含所有共同的HTML结构
- `sidebar.html` - 侧边栏模板
- `search_data.json` - 搜索数据（所有页面的列表）

### 2. 创建转换脚本

创建 `build.py` 脚本，功能：
1. 读取所有MD文件
2. 将MD转换为HTML
3. 使用模板生成完整的HTML文件
4. 更新搜索数据

### 3. 文件结构

```
uem_wiki/
├── templates/
│   ├── base.html          # 基础模板
│   └── sidebar.html       # 侧边栏模板
├── docs/
│   ├── 校园生活/
│   │   ├── 恋爱.md        # MD源文件
│   │   └── 恋爱.html      # 生成的HTML文件
│   └── ...
├── build.py               # 构建脚本
└── search_data.json       # 搜索数据
```

### 4. 模板变量

基础模板中使用以下变量：
- `{{title}}` - 页面标题
- `{{content}}` - 文章内容（HTML格式）
- `{{sidebar}}` - 侧边栏内容
- `{{active_page}}` - 当前页面标识

### 5. 侧边栏管理

侧边栏需要：
1. 自动从文件系统读取所有页面
2. 根据当前页面设置active状态
3. 支持折叠/展开功能

### 6. 搜索数据

创建 `search_data.json` 文件，包含：
```json
[
  { "url": "../校园生活/恋爱.html", "title": "恋爱", "desc": "校园生活 - 恋爱" },
  ...
]
```

## 实现步骤

1. 创建模板目录和基础模板
2. 创建侧边栏模板
3. 创建构建脚本（支持完整功能）
4. 测试转换功能
5. 更新所有现有HTML文件
6. 创建增量构建功能
7. 创建新文件检测功能

## 优势

1. **易于维护** - 只需修改MD文件，运行脚本即可更新
2. **一致性** - 所有页面使用相同的模板，保持风格统一
3. **可扩展** - 新增页面只需添加MD文件，脚本会自动处理
4. **搜索功能** - 自动更新搜索数据，无需手动维护

## 使用方法

```bash
# 构建所有HTML文件
python build.py

# 构建单个文件
python build.py docs/校园生活/恋爱.md
```
